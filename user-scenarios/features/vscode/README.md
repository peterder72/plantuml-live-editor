# VS Code-only scenarios

Put a feature here only when the user-visible capability does not exist in the
standalone web application. Native editor mechanics belong in the VS Code
driver and do not make an otherwise shared scenario VS Code-only.
